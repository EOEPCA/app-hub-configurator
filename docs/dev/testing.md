# Testing

Configurator uses `pytest` and `click.testing.CliRunner`.


## Run tests

```bash
pytest -q
```

## Test categories

* CLI happy path tests (config generated)
* override tests
* manifest dumping tests
* failure cases
* registry tests
