# Remote Desktop profiles

Remote desktop profiles provide a Linux desktop environment via browser (noVNC).

They are based on `BaseRemoteDesktopProfile`.

## Examples

- `remote_desktop`
- `qgis_remote_desktop`
- `panoply_remote_desktop`
- `snap_remote_desktop`

## Default URL

Desktop profiles set:

```python
default_url = "/desktop"
```